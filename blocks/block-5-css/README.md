# блок 5. css

подключил css, сделал разные селекторы, три карточки и пример блочной модели.

где затупил: сначала селекторы выглядели слишком одинаково. отдельно добавил id, класс, вложенный селектор, групповой селектор и `:hover`.

в блочной модели специально разнес `margin`, `padding`, `border`, `width` и `height`, чтобы было видно разницу.
