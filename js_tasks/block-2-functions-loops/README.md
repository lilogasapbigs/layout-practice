# блок 2. функции и циклы

сделал задачи на функции, `return`, циклы, массивы и область видимости.

где затупил: в калькуляторе сначала проще было написать всё в одном обработчике, но по заданию операции должны быть отдельными функциями.

в таблице умножения не забывал очищать старый результат перед новым выводом.
