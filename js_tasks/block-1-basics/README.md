# блок 1. основы js

сделал простые задачи на ввод, условия и boolean-переменную.

где затупил: сначала легко забыть, что значения из input приходят строкой. поэтому в задачах с числами явно использовал `Number`.

ещё отдельно проверял пустые поля, потому что без этого результат получается странным.
