# блок 3. массивы

сделал задачи на перебор, `filter`, `find`, `map`, сортировку и расчёты по массивам.

где затупил: в сортировке важно не портить исходный массив, поэтому перед `sort` делал копию через spread.

для фильтров сделал отдельные функции отрисовки, чтобы не дублировать вывод списков.
